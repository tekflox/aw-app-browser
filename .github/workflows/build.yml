name: Build Browser image

# Builds the Browser Chromium image and pushes it to ghcr.io/<owner>/aw-browser.
# Push builds run only for release bump commits so `latest` follows the tagged
# manifest release. Manual rebuilds remain available via workflow_dispatch.
#
#   gh workflow run build.yml                        # amd64
#   gh workflow run build.yml -f include_arm64=true  # + linux/arm64 (QEMU)
#
# One-time manual step after the FIRST push: the package is created private
# regardless of this repo's own visibility, and GitHub has no REST/GraphQL
# endpoint to change a container package's visibility (confirmed 2026-07-28:
# PATCH /orgs/tekflox/packages/container/aw-browser -> 404, with a token that
# *can* GET the package fine, so it's not a scope issue — the endpoint simply
# doesn't exist for packages). This blocks the "public app = tokenless
# install" policy (aw-workspace-apps-public-policy) until someone with org
# admin flips it by hand:
#   github.com/orgs/tekflox/packages/container/package/aw-browser
#   -> Package settings -> Danger Zone -> Change visibility -> Public
# Repeat for any future app repo that publishes its own container image.
on:
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      include_arm64:
        description: 'Also build linux/arm64 (QEMU emulation; slower)'
        type: boolean
        default: false

concurrency:
  group: build-aw-browser-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build:
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && contains(github.event.head_commit.message, '[skip release]')) }}
    name: aw-browser
    runs-on: [self-hosted, aw-baremetal]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # ghcr.io rejects mixed-case namespaces — lowercase the owner login.
      - name: Lowercase repo owner
        id: owner
        run: echo "lc=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"

      - name: Resolve platforms
        id: plat
        run: |
          if [ "${{ inputs.include_arm64 }}" = "true" ]; then
            echo "platforms=linux/amd64,linux/arm64" >> "$GITHUB_OUTPUT"
          else
            echo "platforms=linux/amd64" >> "$GITHUB_OUTPUT"
          fi

      - name: Read app version
        id: version
        run: |
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          print(f"version={json.load(open('aw-app.json'))['version']}")
          PY

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: container
          file: container/Dockerfile
          platforms: ${{ steps.plat.outputs.platforms }}
          push: true
          tags: |
            ghcr.io/${{ steps.owner.outputs.lc }}/aw-browser:latest
            ghcr.io/${{ steps.owner.outputs.lc }}/aw-browser:v${{ steps.version.outputs.version }}
            ghcr.io/${{ steps.owner.outputs.lc }}/aw-browser:${{ github.sha }}
          cache-from: type=registry,ref=ghcr.io/${{ steps.owner.outputs.lc }}/aw-browser:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ steps.owner.outputs.lc }}/aw-browser:buildcache,mode=max

      - name: Summary
        run: |
          {
            echo "## 🐳 aw-browser pushed"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| **Image** | \`ghcr.io/${{ steps.owner.outputs.lc }}/aw-browser:latest\` |"
            echo "| **Version tag** | \`ghcr.io/${{ steps.owner.outputs.lc }}/aw-browser:v${{ steps.version.outputs.version }}\` |"
            echo "| **Platforms** | ${{ steps.plat.outputs.platforms }} |"
          } >> "$GITHUB_STEP_SUMMARY"
